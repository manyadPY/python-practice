### تمرین ۱

دو متغیر زیر را ایجاد کنید:

a = 15

b = 5

نتیجه موارد زیر را چاپ کنید:

    جمع
    تفریق
    ضرب
    تقسیم


### پاسخ

```python

a = 15
b = 5

print("+", a + b)
print("-", a - b)
print("*", a * b)
print("/", a / b)

```

<br />

---

### تمرین ۲

یک متغیر به نام score با مقدار 80 ایجاد کنید.

با استفاده از یکی از عملگر های انتساب، مقدار آن را 10 واحد افزایش دهید.

سپس نتیجه را نمایش دهید.

### پاسخ

```python

score = 80
score += 10

print(score)

```

---

<br />



### تمرین 3

دو متغیر زیر را ایجاد کنید:

age = 36

height = 1.84

بررسی کنید آیا مقدار age بزرگ تر از 18 است یا خیر.

### پاسخ:

 ```python

age = 36
height = 1.84

print(age > 18)

```

---

<br />



### تمرین 4

دو متغیر زیر را ایجاد کنید:

age = 36

country = "Iran"

یک عبارت مقایسه ای بنویسید که خروجی آن True باشد.

سپس یک عبارت دیگر بنویسید که خروجی آن False باشد.


### پاسخ:

```python

age = 36
country = "Iran"

print(age > 2)

print(country == "pelestin")

```

---

<br />



### تمرین 5

عبارت زیر را با استفاده از پرانتز طوری باز نویسی کن که ترتیب اجرای عملیات کاملا مشخص باشد:

result = 10 + 5 * 2 > 15 and 20 % 3 == 2

### پاسخ:

```text

در سمت چپ عملگر and یک عبارت مقایسه ای داریم که پاسخ آن True است و در سمت راست هم یک عبارت مقایسه ای دیگر داریم که پاسخ آن هم True است و از آنجایی که در عملگر and هر 2 شرط صحیح میباشند نتیچه ی نهایی True است.

```
---


<br />


### پروژه کوچک — ماشین حساب ساده

در این پروژه یک ماشین حساب ساده می سازید.

برنامه باید بتواند:

    دو عدد از کاربر دریافت کند.
    عملیات ریاضی انجام دهد.
    نتیجه را نمایش دهد.

هدف پروژه

برنامه باید محاسبات زیر را انجام دهد:

    جمع
    تفریق
    ضرب
    تقسیم


### پاسخ:

```python

number1 = float(input("please enter number 1:" ))
number2 = float(input("please enter number 2:" ))
print("Addition:", number1 + number2)
print("Subtraction:" , number1 - number2)
print("Multiplication:" , number1 * number2)
print("Division:" , number1 / number2)


```

---

<br />


### پروژه کوچک: سیستم احراز هویت کاربر

بعد از یادگیری انواع مختلف شرط ها، وقت آن است که آن ها را در یک پروژه کوچک کنار هم استفاده کنیم.

در این پروژه یک سیستم ساده احراز هویت کاربر ایجاد می کنیم.

برنامه چند شرط مختلف را بررسی می کند و تصمیم می گیرد که آیا کاربر اجازه ورود دارد یا نه.
معرفی پروژه

یک وب سایت را تصور کنید که کاربران برای ورود به آن باید اطلاعات خود را وارد کنند.

قبل از اجازه دسترسی، سیستم باید موارد زیر را بررسی کند:

    آیا کاربر وجود دارد؟
    آیا رمز عبور درست است؟
    آیا حساب کاربری فعال است؟

فقط زمانی که شرط های لازم برقرار باشند، کاربر می تواند وارد حساب شود.
نیازمندی های پروژه

برنامه ما باید:

    بررسی کند نام کاربری درست است یا نه.
    بررسی کند رمز عبور درست است یا نه.
    وضعیت فعال بودن حساب را بررسی کند.
    پیام مناسب را نمایش دهد.

### پاسخ:

```python

user = True
password = True
is_active = True


if user and password and is_active:
     print("loggining")
else:
    print("incorect")

```

---

<br />


<!-- ```python



```

---

<br /> -->